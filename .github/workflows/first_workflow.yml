name: First Workflow
on:
  push:
    tags:
      - 'image-release'

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display text
        run: echo "Some text"
      - name: Command in Multiline
        run: |
          node -v
          npm -v
      - name: Command in Multiline 2
        run: |
          node -v
          npm -v          
